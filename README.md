# TestFirstWeb

TestFirstWeb — это веб-приложение на Django, предназначенное для управления каталогом аренды автомобилей, запчастей и блогов. Приложение предоставляет возможности для просмотра, фильтрации и детального изучения продуктов и блог-постов.

## Содержание

- [Установка](#установка)
- [Модели](#Модели)
- [Функции](#функции)
- [Описание папок](#описание)
- [URL-шаблоны административной панели](#url-шаблоны-административной-панели)

## Установка

Чтобы настроить этот проект на вашем локальном компьютере, выполните следующие шаги:

1. **Клонируйте Репозиторий**

   ```bash
   git clone https://github.com/Zhaanibek/testFirstWeb.git
   cd testFirstWeb

2. **Создайте Виртуальное Окружение**

   ```bash
   python -m venv venv
   ```

3. **Активируйте Виртуальное Окружение**

   - На Windows:
     ```bash
     venv\Scripts\activate
     ```

   - На macOS/Linux:
     ```bash
     source venv/bin/activate
     ```

4. **Примените Миграции**

   ```bash
   python manage.py migrate
   ```

5. **Запустите Сервер Разработки**

   ```bash
   python manage.py runserver
   ```

   Откройте `http://127.0.0.1:8000/` в вашем веб-браузере, чтобы увидеть приложение в действии.

## Модели

Вот пример README-файла на русском языке для вашего Django проекта, включающий описание моделей и их административных интерфейсов:

---

# Django Проект

## Обзор

Этот проект Django предназначен для управления блогом и магазином с продуктами и категориями. Проект включает в себя модели для блога, такие как категории и посты, а также для магазина, включая аренду автомобилей и запасные части.

## Модели

### Модели блога

#### CategoryBlog
Модель для управления категориями блога. Имеет следующие поля:
- `name` - название категории
- `slug` - уникальный идентификатор для URL

#### Post
Модель для управления постами блога. Имеет следующие поля:
- `title` - заголовок поста
- `category` - категория поста
- `slug` - уникальный идентификатор для URL
- `description` - содержание поста
- `image` - изображение поста
- `author` - автор поста (ссылка на пользователя)
- `created_at` - дата создания поста

### Модели магазина

#### CategoryRental
Модель для управления категориями аренды автомобилей. Имеет следующие поля:
- `name` - название категории
- `slug` - уникальный идентификатор для URL

#### CategorySpare
Модель для управления категориями запасных частей. Имеет следующие поля:
- `name` - название категории
- `slug` - уникальный идентификатор для URL

#### ProductCarRental
Модель для управления продуктами аренды автомобилей. Имеет следующие поля:
- `name` - название продукта
- `category` - категория аренды
- `slug` - уникальный идентификатор для URL
- `description` - описание продукта
- `image` - изображение продукта
- `price` - цена продукта
- `quantity` - количество продукта
- `in_stock` - наличие на складе

#### ProductSpare
Модель для управления запасными частями. Имеет следующие поля:
- `name` - название продукта
- `category` - категория запасной части
- `slug` - уникальный идентификатор для URL
- `description` - описание продукта
- `image` - изображение продукта
- `price` - цена продукта
- `quantity` - количество продукта

## Административный интерфейс

### Блог

- **CategoryBlogAdmin**: Управление категориями блога.
  - Поля для редактирования: `name`, `slug`
  - Выводимые поля: `name`
  
- **PostAdmin**: Управление постами блога.
  - Поля для редактирования: `title`, `category`, `slug`, `description`, `image`, `author`
  - Выводимые поля: `title`, `author`, `created_at`
  - Поиск: `title`, `description`, `author`
  - Фильтрация: `category`, `author`

### Магазин

- **CategoryRentalAdmin**: Управление категориями аренды автомобилей.
  - Поля для редактирования: `name`, `slug`
  - Выводимые поля: `name`
  
- **CategorySpareAdmin**: Управление категориями запасных частей.
  - Поля для редактирования: `name`, `slug`
  - Выводимые поля: `name`

- **ProductCarRentalAdmin**: Управление продуктами аренды автомобилей.
  - Поля для редактирования: `name`, `category`, `slug`, `description`, `image`, `price`, `quantity`, `in_stock`
  - Выводимые поля: `name`, `quantity`, `price`, `in_stock`
  - Поиск: `name`, `description`
  - Фильтрация: `quantity`, `category`, `in_stock`

- **ProductSpareAdmin**: Управление запасными частями.
  - Поля для редактирования: `name`, `category`, `slug`, `description`, `image`, `price`, `quantity`
  - Выводимые поля: `name`, `quantity`, `price`
  - Поиск: `name`, `description`
  - Фильтрация: `quantity`, `category`

### Функции

- **Каталог Аренды Автомобилей**: Просмотр и фильтрация продуктов аренды автомобилей по категориям.
- **Каталог Запчастей**: Просмотр и фильтрация запчастей по категориям.
- **Блог Посты**: Чтение и исследование различных блог-постов, сгруппированных по темам.


## Описание 

Ниже приведено объяснение структуры папок проекта, особенно `Static deps` и `Media`.

### `Static deps`

Папка `Static deps` предназначена для хранения статических файлов, которые используются для стилизации и улучшения интерфейса приложения. Эти файлы включают:

- **CSS файлы** — используются для стилизации страниц.
- **Иконки** — графические элементы, которые могут использоваться в интерфейсе.
- **JavaScript файлы** — скрипты для добавления интерактивности и улучшения функциональности приложения
  
- `.css` — основной файл стилей.
- `.js` — основной JavaScript файл.
- `icons/` — папка с иконками.

### `Media`

Папка `Media` используется для хранения файлов, загруженных через административную панель Django:

- **Изображения** — фотографии и графика, загруженные пользователями или администраторами.


  Вот пример документации для файла `README.md`, объясняющий URL-шаблоны административной панели Django:

---

## URL-шаблоны административной панели

#### 1. `http://127.0.0.1:8000/admin/blog/categoryblog/`
- **Описание:** Страница для просмотра списка всех объектов модели `CategoryBlog` в административной панели.
- **Методы:** GET

#### 2. `http://127.0.0.1:8000/admin/blog/categoryblog/add/`
- **Описание:** Форма для добавления нового объекта модели `CategoryBlog`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 3. `http://127.0.0.1:8000/admin/blog/categoryblog/3/change/`
- **Описание:** Форма для редактирования объекта модели `CategoryBlog` с идентификатором `3`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)

#### 4. `http://127.0.0.1:8000/admin/blog/post/`
- **Описание:** Страница для просмотра списка всех объектов модели `Post`.
- **Методы:** GET

#### 5. `http://127.0.0.1:8000/admin/blog/post/add/`
- **Описание:** Форма для добавления нового объекта модели `Post`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 6. `http://127.0.0.1:8000/admin/blog/post/1/change/`
- **Описание:** Форма для редактирования объекта модели `Post` с идентификатором `1`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)

#### 7. `http://127.0.0.1:8000/admin/goods/productspare/`
- **Описание:** Страница для просмотра списка всех объектов модели `ProductSpare`.
- **Методы:** GET

#### 8. `http://127.0.0.1:8000/admin/goods/productspare/add/`
- **Описание:** Форма для добавления нового объекта модели `ProductSpare`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 9. `http://127.0.0.1:8000/admin/goods/productspare/1/change/`
- **Описание:** Форма для редактирования объекта модели `ProductSpare` с идентификатором `1`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)

#### 10. `http://127.0.0.1:8000/admin/goods/categoryspare/`
- **Описание:** Страница для просмотра списка всех объектов модели `CategorySpare`.
- **Методы:** GET

#### 11. `http://127.0.0.1:8000/admin/goods/categoryspare/add/`
- **Описание:** Форма для добавления нового объекта модели `CategorySpare`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 12. `http://127.0.0.1:8000/admin/goods/categoryspare/4/change/`
- **Описание:** Форма для редактирования объекта модели `CategorySpare` с идентификатором `4`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)

#### 13. `http://127.0.0.1:8000/admin/goods/productcarrental/`
- **Описание:** Страница для просмотра списка всех объектов модели `ProductCarRental`.
- **Методы:** GET

#### 14. `http://127.0.0.1:8000/admin/goods/productcarrental/add/`
- **Описание:** Форма для добавления нового объекта модели `ProductCarRental`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 15. `http://127.0.0.1:8000/admin/goods/productcarrental/1/change/`
- **Описание:** Форма для редактирования объекта модели `ProductCarRental` с идентификатором `1`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)

#### 16. `http://127.0.0.1:8000/admin/goods/categoryrental/`
- **Описание:** Страница для просмотра списка всех объектов модели `CategoryRental`.
- **Методы:** GET

#### 17. `http://127.0.0.1:8000/admin/goods/categoryrental/add/`
- **Описание:** Форма для добавления нового объекта модели `CategoryRental`.
- **Методы:** GET (отображение формы), POST (отправка данных формы)

#### 18. `http://127.0.0.1:8000/admin/goods/categoryrental/1/change/`
- **Описание:** Форма для редактирования объекта модели `CategoryRental` с идентификатором `1`.
- **Методы:** GET (отображение формы), POST (отправка измененных данных)
